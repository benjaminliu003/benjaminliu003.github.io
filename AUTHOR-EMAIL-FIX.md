# Git Author Email Correction

A typo in the git author/committer email for `benjaminliu003` was corrected.

- Incorrect (typo): `benjmain.liu@outlook.com`
- Correct:          `benjamin.liu@outlook.com`

On 2026-06-26 the repository history was rewritten to replace the typo'd
email in both the author and committer fields of every affected commit,
across all local branches and tags. The rewrite changed commit metadata
only; no tracked file contents were altered by it.

If this repository has a remote, the rewritten history will require a
force-push; pull/rebase and coordinate as needed before pushing.
